stages:
  - build
build:
  stage: build
  script:
    - sudo /www/server/nvm/versions/node/v12.18.1/bin/npm install --registry=https://registry.npm.taobao.org
    - sudo npm run build
    - cp -r dist /www/wwwroot/
